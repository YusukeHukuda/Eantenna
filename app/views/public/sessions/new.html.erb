<div class="container mt-5">
  <%= render "public/shared/error_messages", resource: resource %>
  <%= form_with model: @user, url: user_session_path do |f| %>
    <div class="row mt-5">
      <div class="form-group col-md-2 offset-md-2">
        <%= f.label :email,"メールアドレス" %>
      </div>
      <div class="form-group col-lg-4">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control border-dark" %>
      </div>
    </div>

    <div class="row">
      <div class="form-group col-md-2 offset-md-2">
        <%= f.label :password,"パスワード" %>
      </div>
      <div class="form-group col-lg-4">
        <%= f.password_field :password, autocomplete: "current-password", class: "form-control border-dark" %>
      </div>
    </div>

    <div class="actions">
      <div class="row justify-content-center">
        <div class="col-lg-3 mt-4 text-center"><%= f.submit "Log in", style: 'width:150px', class: "btn btn-warning" %></div>
      </div>
    </div>
  <% end %>
</div>

<div class="container mt-5">
  <p class="text-center">
    初めて「E.antenna」を利用する方へ</p>
  <p class="text-center text-secondary">
    すべての機能を利用するためには、会員登録（無料）していただく必要があります。</p>
  <div class="d-flex justify-content-center">
    <buttontype="button" class="btn btn-warning">
      <%= render "public/shared/links" %>
    </button>
  </div>
</div>